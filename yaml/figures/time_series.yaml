name: time_series
version: 1
params:
  legend_loc: upper left
figure:
  layout: [1,1]
  title: "{title}"
  output name: "{output}"
plots:
  - add_xlabel: "Datetime"
    add_ylabel: "{y_label}"
    add_grid:
    add_legend: { loc: "{legend_loc}" }
    layers: []
